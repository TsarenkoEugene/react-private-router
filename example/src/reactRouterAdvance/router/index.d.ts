/// <reference types="react" />
declare const CustomRoute: ({ path, component, redirectUrl, guards, resolvers, debounceWaitTime, childs, redirectToChild, location, }: import("./types").OwnProps) => JSX.Element | null;
export default CustomRoute;
